.. _components_quality_monitor:

===============
Quality Monitor
===============


TRL
==============
`low-tmc/subarray-quality-monitor/01`

.. note:: In the full scale deployment, there are 16 instances of Quality 
   Monitor one per Subarray. In such case, only the last part of the TRL 
   indicates the id of the subarray. E.g. `low-tmc/subarray-quality-monitor/02`, 
   `low-tmc/subarray-quality-monitor/03` and so on.

Attributes
==============

.. csv-table:: Attribute List
   :file: data/quality_monitor.csv
   :header-rows: 1